What is the command to initialize a new Git repository? 
2
git clone
git init
git commit
git status

What is the command to check the current status of the repository? 
4
git status
git init
git log
git push

How can you create a new branch in Git? 
3
git branch <branch_name>
git checkout -b <branch_name>
git merge <branch_name>
git commit -m "new branch"

Which of the following commands stages changes in Git? 
4
git add .
git commit
git push
git status

What command would you use to add a remote repository in Git? 
3
git remote add <name> <url>
git clone <url>
git fetch
git pull

What command is used to commit changes in Git? 
2
git commit -m "message"
git push
git merge
git pull

What does the `git pull` command do? 
1
Fetches and merges changes from the remote repository
Fetches changes from the local repository
Only commits local changes
Pushes changes to the remote repository

What is GitHub? 
2
A platform for hosting Git repositories
A text editor for coding
A local repository manager
A command-line interface for Git

Which command is used to upload changes from the local repository to the remote repository? 
1
git push
git fetch
git pull
git clone

What is the purpose of a `.gitignore` file? 
3
To exclude files from being tracked by Git
To include files in the commit history
To configure Git username and email
To store commit messages